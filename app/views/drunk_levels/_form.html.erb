<%= form_for(@drunk_level) do |f| %>
  <% if @drunk_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drunk_level.errors.count, "error") %> prohibited this drunk_level from being saved:</h2>

      <ul>
      <% @drunk_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :icon %><br>
    <%= f.text_field :icon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
