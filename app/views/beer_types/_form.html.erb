<%= form_for(@beer_type) do |f| %>
  <% if @beer_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beer_type.errors.count, "error") %> prohibited this beer_type from being saved:</h2>

      <ul>
      <% @beer_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
